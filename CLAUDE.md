# CLAUDE.md

このリポジトリで作業するときの規約。

---

## 処理結果の報告

**依頼を1つ完了するごとに、処理結果を md ファイルにまとめて添付で渡すこと。**

- **リポジトリにはコミットしない。** 報告書は履歴として残さず、ファイルとして渡すだけ。
  `docs/` にも置かない（`docs/` は設計文書と一次資料の置き場）。
- 章立ては次の4つに固定する。

| 章 | 書くこと |
|---|---|
| 1. 依頼 | 何を頼まれたか。制約・禁止事項があればそれも |
| 2. 変更 | 実際に変更したファイルと `git diff --stat`。成果物の構成 |
| 3. 確認 | `npm run check` の結果。記載内容をどう裏取りしたか。執筆中に自己修正した点 |
| 4. 読み取れなかったこと | 根拠が無く保留した点 |

### 報告書に適用する原則

**確認したことと、確認していないことを混ぜない。**

- 検査名・件数・数値は、**実行結果か実データから取る。記憶や推測で書かない。**
- 根拠が無いことは「読み取れない」と書いて残す。「〜と思われる」「〜のはず」で
  埋めない。これは `docs/fr-e700-to-e800-replacement.md` の「取り扱いの絶対ルール」と
  同じ方針を、報告書にも適用するもの。
- 裏取りの方法（どのコマンドを実行したか、どのファイルを読んだか）を書く。
  後から検算できるようにするため。

---

## コード・データの規約

詳細は `README.md` の「データの約束」を参照。作業前に必ず読むこと。
ここには、そこに書かれていない運用面だけを書く。

### 検証

- 変更後は必ず `npm run check` を **exit=0** で通す。
  内訳は `verify-data` → `verify-legacy` → `build` → `regress` →
  `test-compat` → `smoke` → `test:ext`。
- `smoke` は playwright を使う。未インストールなら `npm install` を実行する
  （`node_modules/` は `.gitignore` 済み）。
- **検査を削除・無効化して通さない。** 落ちたら原因を直す。

### コメント

**このリポジトリは「なぜそうしたか」をコメントに残す方針で書かれている。**
何をしているかではなく、なぜその選択をしたか・何を防いでいるかを書く。
既存の JSDoc（`src/core/ui.mjs`・`tools/verify-legacy.mjs` など）が見本。

### 検査を足すとき

検査名はそのまま仕様の記述になる。`npm run check` の出力に並ぶので、
何を守っているかが名前だけで分かるようにする。

- 対象が0件でも「全項目PASS」と出る検査を作らない
  （`verify-legacy` 検査1 がこの穴を塞いだ前例）。
- 「解析できないから一致とみなす」経路を作らない。書式違いは黙って読み飛ばさず
  NG にする（`tools/verify-legacy.mjs:326`）。
- 色コードなど、テーマ側と二重管理になる値を検査に直書きしない
  （`tools/smoke.mjs:276-278`）。

---

## Git

- 作業ブランチは依頼ごとに指定される。**指定外のブランチへ push しない。**
- PR は明示的に依頼されたときだけ作る。
- 1つのPRに複数の関心事を混ぜない。設計文書のPRにコード変更を混ぜない。
- **差分を確認するときは `origin/main` と比較する。**
  ローカルの `main` は古い状態で止まっていることがある。
  `git diff --stat main...HEAD` が 106,000行・56ファイルという差分を出したことがあり、
  そのときローカル `main` は `a11f5e2`（PR #13〜#16 のマージを含まない状態）を
  指していた。PR #13 の監査時にも同じ `a11f5e2` で同じことが起きているので、
  1回限りの事故ではなく再現する環境の癖として扱う。
