# 引き継ぎ書 v2：電気保全向け互換検索アプリ magnet-compat

新しいチャットの冒頭でこのファイルを渡してください。
**v1（前回の引き継ぎ書）はこのファイルで置き換わります。v1は破棄してください。**

最終更新：2026-08-20

---

## 0. あなた（Claude）の役割

このプロジェクトでは、あなたは **Claude Code への指示を設計し、返ってきた結果を審査する側** です。
コードを直接書くのではなく、次の4つを担当します。

### ① Claude Code に送るプロンプトを作る

ユーザーはiPhoneのみで作業しており、長文入力が困難です。
「〜してください」と方針を伝えるのではなく、**そのままコピペできる完成形のプロンプト**を
コードブロックで提示してください。以下を必ず含めます。

- 何を変更し、何を変更してはいけないか
- 守るべき制約（後述の「絶対ルール」）
- 範囲の限定（一度に大きくやらせない。差分が数百行を超えるとiPhoneでレビュー不能）
- 検証方法（`npm run check` を通すこと、壊したら落ちることを確認させること）
- 出力形式（まず方針だけ／実装してPR作成、など）

### ② 返ってきた結果を審査する

ユーザーはClaude Codeの応答をスクリーンショットで送ってきます。
**そのまま承認せず、次を必ず確認**してください。

- 指示していない値が勝手に埋まっていないか（特に出典なきスペック値）
- 「テストが通った」だけでなく「壊したら落ちること」を確認しているか
- 報告の中に、指示していない改善や問題提起が含まれていないか
- **ユーザー（および前任のあなた）の指示が間違っていた可能性はないか**

最後の項目は形式的な注意書きではありません。**2026-08-20 のセッションだけで、
Claude Code が前任のClaudeの誤りを4回訂正しています**（詳細は §6）。
指示に合わせて辻褄を合わせず、実測に基づいて訂正してくる挙動は正しいので、
それを潰さないでください。

良い点は具体的に指摘し、懸念があれば率直に伝えてください。
**追加0件・変更なしという結果が「正しい結果」である場合があります**（§2）。

### ③ 外部情報の取得を代行する

Claude Code のクラウドセッションは **外部ネットワークが完全に遮断** されています。
メーカーの公式サイトもカタログPDFも一切開けません。

そのため、**メーカー仕様の調査はあなた（このチャット）が担当**します。
実際にページを取得して確認した内容だけを、Markdownファイルにまとめてユーザーに渡し、
ユーザーがGitHubにアップロード → Claude Code がそれを読んで投入、という流れです。

まとめる際は必ず以下を明記します。

- 出典URL / カタログ番号 / 該当ページ / 取得日
- **取得できなかった項目を「未取得」として明示**（推測で埋めない）
- 表の値と独立に検算できる材料があれば併記する

**取得の実務メモ（2026-08-20 時点で確認済み）**

- 三菱FA「置換え資料」（資料番号 `BCN-C21002-nnn`）は **ログイン不要で取得できる**
- ただし「カタログ」本体（例：FR-E800カタログ L-06130-H）は **42MB あり、
  web_fetch のサイズ上限を超えて取得できない**
- 置換え資料の一覧ページ：
  `https://www.mitsubishielectric.co.jp/fa/products/drv/inv/ex/renew/renewal/index.html`
- 直リンクの例（実際に取得成功）：
  `https://www.mitsubishielectric.co.jp/fa/document/others/inv/bcn-c21002-214/bcn-c21002-214c.pdf`

### ④ GitHub操作の手順を案内する

ユーザーはiPhoneのブラウザでGitHubを操作します。
「〜をタップ」レベルの具体的な手順で案内してください。

---

## 1. プロジェクトの目的

**電気設備の故障時、復旧時間を短縮すること。**

50年以上前の設備が稼働している現場では、故障した機器が製造中止になっていることが多い。
後継機種や同等機能品を探し、接続方法とパラメータを試行錯誤しながら復旧するため、
長時間停止するケースが多い。この試行錯誤を減らすためのアプリ。

対象カテゴリ：電磁接触器、電磁開閉器、サーマルリレー、インバータ、サーボユニット、
近接スイッチ、フォトスイッチ、超音波センサ、特殊（熱間/光学/導光）の9カテゴリ。

**目的から導かれる重要な含意**：現行品を検索できても復旧時間は縮まりません。
**製造中止品から後継に辿り着けること**が、このアプリの存在意義です。
2026-08-20 の作業は、この一点に向けたものです。

---

## 2. 絶対ルール（これを破ると人が怪我をする）

このアプリは、**設備が止まっている現場で、人が型番を見て発注する**道具です。
存在しない型番を出せば復旧はかえって遅れ、誤った機器を入れれば事故につながります。

1. **出典が確認できない型式は絶対に追加しない**
2. **型番を推測・生成しない**
   - 「S-T10 があるなら S-T13 も」という補完が、過去に **1440件の架空型式** を生んだ
   - 実データは1467件中27件だけで、残り98.2%が組み合わせ生成の産物だった
3. **未取得の項目はキーごと不在にする**（`0` や空文字で埋めない）
4. **記憶や一般知識で補わない**。公式資料に書いてあるかだけで判断する
5. **迷ったら追加せず報告する**

### 「追加0件」は失敗ではない

compat-researcher に三菱インバータの調査を指示したところ、クラウド環境から外部に
アクセスできず、検索結果のタイトルとURLしか取れなかった。エージェントは
「そのURLにページがあるらしい、という示唆にすぎない」と判断し、**追加0件で戻ってきた**。
これは正しい挙動です。

---

## 3. 現状

### リポジトリ
`wyv2d2ms4p-cpu/magnet-compat`（Public）

### 構成
```
data/*.json          カテゴリ別データ
data/legacy/*.tsv    旧機種の後継対応表（4ファイル30行）
data/_seed/          未昇格の生成データ（1440件、provisional）
data/reference/      デバイスでない参照情報
src/core/            util.mjs、registry.mjs、compat.mjs、store.mjs、schema-map.mjs
src/categories/*.mjs カテゴリ別の互換判定ルール（インバータは drive.mjs）
src/app.mjs, ui.mjs  画面
tools/               verify-data.mjs、verify-legacy.mjs、smoke.mjs、test-compat.mjs、
                     regress.mjs、test-extensibility.mjs、extract.mjs
build.mjs            単一HTML化（dist/index.html）
docs/                設計文書・公式データ
.claude/agents/      code-reviewer、compat-researcher
```

### 配布形態
GitHub Pages で公開 → iPhoneのホーム画面に追加して現場で使用。
```
https://wyv2d2ms4p-cpu.github.io/magnet-compat/dist/index.html
```
**オフライン動作が必須**のため、`build.mjs` で単一HTMLにインライン展開。
`build.mjs:174-183` が `fetch()` の混入を明示的に禁止しているので、
**dist から外部ファイルを後読みする手段は原理的に存在しません。**

### 検査（`npm run check`）
- `verify-data.mjs` 14項目
- **`verify-legacy.mjs` 7項目**（2026-08-20 に新設）
- 回帰チェック（`regress.mjs`。CATEGORIES に inverter は含まれないので影響なし）
- 互換判定テスト（`test-compat.mjs`）
- スモークテスト（`smoke.mjs`）
- 拡張性テスト（`test-extensibility.mjs`）6項目

**この検査群が最大の資産です。**
プロンプトの指示だけに頼ると、エージェントが状況次第で逸脱しても検出できないため、
**約束は必ず検査として実装する**方針を取っています。

---

## 4. 2026-08-20 に実施したこと

### 4-1. 実機での動作確認（完了）

| 確認 | 結果 |
|---|---|
| `FR-E820-3.7K-1`（17.5A）で候補が出るか | ○ FR-E820-5.5K-1（24A）が1件 |
| `FR-E820S-1.5K-1`（単相）に三相が混ざらないか | ○ 混ざらない |
| **逆方向**：`FR-E820-1.5K-1`（三相）に単相が混ざらないか | ○ 混ざらない |
| 同綴り衝突（1.5K / 15K）の警告表示 | ○ 適用モータ容量が併記され警告が出る |

### 4-2. 公式資料の取得（完了）

`docs/fr-e700-to-e800-replacement.md` を作成し `main` にマージ済み。

- 出典：**BCN-C21002-214C**「FREQROL-E700 シリーズから FREQROL-E800 シリーズへの
  置換え資料」（全34ページ、取得日 2026-08-20）
- 内容：4電圧クラス計37機種の1対1対応表、取付互換アタッチメントの要否、
  FR-E800 の外形寸法・取付寸法、パラメータ移行の要注意点
- 補助出典：セールスとサービス No.856（FR-E700-NC 生産終了、2026年3月）

### 4-3. PR #10：FR-E720 3相200V 11機種を `data/legacy` に登録（マージ済み）

宙に浮いていたブランチ `claude/fr-e800-ratings-data-3roe9c`（ヘッダー行のみ・
登録0件）を回収し、そこに中身を入れた。

### 4-4. PR #11：`tools/verify-legacy.mjs` を新設（マージ済み）

`data/legacy/*.tsv` はどの検査からも読まれておらず、`npm run check` が通っても
内容が一切保証されていない状態だった。7項目の検査を追加。

**8通りの壊し方すべてで `exit=1` になることを確認済み。**
毎回 `git checkout` で復元しハッシュ一致まで検証している。

Claude Code が自主的に「`.tsv` が1つ以上あること」を先頭に追加した（対象0件でも
「全項目PASS」と出ると検査の意味が消えるため）。これは前任Claudeの設計の穴だった。

### 4-5. PR #12：残り19機種を `data/legacy` に登録（マージ済み）

3相400V 9件、単相200V 6件、単相100V 4件。3ファイルに分割。
**19行すべての `successors` を一時的に実在しない値に書き換えて再実行し、
検査が19件ちょうど検出することを確認**（＝行がスキップされて空振りしていない証明）。

### 4-6. 設計調査（プランモード、完了）

「旧機種から後継に辿り着けるようにする」ための調査。31コマンドを実行し、
ファイルは1件も変更していない。→ **案C（推奨）** が採択。

### 4-7. 段1+2 の実装（**要審査**）

**注意：この項目はスクリーンショットの一部が見えただけで、前任Claudeは正式に
審査していません。次のチャットで最初にやるべきことは、この結果の審査です。**

読み取れた範囲：

- インバータ件数 **40 → 70件**（旧機種30件を追加）
- `npm run check` 全項目PASS
  （verify 14/14、verify:legacy 7/7、regress、test-compat **27/27**、
  smoke **63/63**、test:ext 6/6）
- 検査8（successorId の参照解決）：OK — 30件すべて解決
- 検査13（正規化衝突）：OK
- **削除した検査はゼロ**（test-compat 23→27項目、smoke 62→63 と項目が増えている）
- `FR-E720-3.7K` 基準 → 候補は `FR-E820-3.7K-1` の1件だけ。
  `dist/index.html` を実際に開いて確認。「生産終了」バッジ・note・後継品リンクが
  出て、定格は「—」表示
- `FR-E820-3.7K-1` 基準 → 候補は `FR-E820-5.5K-1` のみ、旧機種は混ざらない
- 「`ratedCurrentA`/`ratedCapacityKVA` をキーごと持たず `ratedPowerKW` だけ持つ」
  という検査を新設
- 電流コード検算は対象を限定（旧機種の形名に4桁コードが無いため）。
  CURRENT_CODE の40件は不変
- ±30%窓・対称性の2件は後継品リンクの辺を対象外にし、代わりに
  「旧機種の候補は後継品ちょうど1件」を追加

**未確認の懸念（スクショに写っていた）**：
`_seed` の検索衝突警告が **199 → 213組** に増えた。`_seed` に同じ形名の生成データが
18件あるため。警告のみでPASSしているが、**次チャットで内容を確認すべき。**

また `src/categories/drive.mjs` に「現在は…40機種」というコメントが残っている
可能性がある（スクショが途切れており未確認）。

---

## 5. これからやること：設計案C（段3〜6）

### 案Cとは

旧機種を `data/inverter.json` の正式レコードに昇格させたうえで、
**③互換品を表示の画面を2ゾーンに分割する**。

```
③ 互換品を表示
FR-E720-3.7K [生産終了]  三菱電機

■ メーカー指定の後継品
  FR-E820-3.7K-1                    17.5A
  出典 BCN-C21002-214C p.2
  ⚠ 置換えに FR-E8AT03（取付互換アタッチメント）が必要
  ⚠ Pr.570 を ND定格に。Pr.800 / Pr.162 も再設定

■ 同じ容量帯の型式（当アプリの判定）
  メーカーが後継として指定したものではありません。
  容量が足りない機種も含みます。
```

### なぜ分けるのか（最重要）

**現在の互換判定は「定格電流が足りる後継を出す」ではありません。**

`withinWindow`（compat.mjs:13-16）は `|a-b| ≤ max(a,b)*0.3` の**対称な窓**で、
「1サイズ上まで」の制限は実装されていません。40件全通しの実測では、
**候補30件のうち15件が基準より定格電流が小さい**。
例：FR-E820-5.5K-1（24A）の候補は 3.7K-1（17.5A）と 7.5K-1（33A）の両方。

つまり「互換品候補」という見出しの下に、**既に容量不足の機種が半分並んでいます。**
そこに公式後継が「No.1」として並ぶと「ダメならNo.2で代用」という読みを誘発し、
**復旧を早めるどころか焼損する置換えを提案しうる構造**になります。

公式後継には条件が付いています（FR-E8AT03 が必要、Pr.570/800/162 の再設定）。
同格の候補の1つではなく、**手順を伴う1本の正解**です。

### 残りの段取り

| 段 | 内容 | 対象ファイル | 状態 |
|---|---|---|---|
| 1 | 旧機種30件を `data/inverter.json` に昇格 | data/ | **要審査** |
| 2 | 件数直書きテストの更新 | tools/ | **要審査** |
| 3 | ③ヘッダに `statusBadges`、②に `discontinuedNote` | app.mjs, ui.mjs | 未着手 |
| 4 | ③を2ゾーンに分割 | app.mjs | 未着手 |
| 5 | インバータに `emptyNote` 実装 | drive.mjs | 未着手 |
| 6 | 旧機種の検索・後継到達・生産終了表示のスモーク追加 | smoke.mjs | 未着手 |

**1段ずつ別PRにしてください。** まとめるとiPhoneでレビューできません。

### 段3〜6 の実装メモ（調査で判明済み）

**段3**：`viewResult` の結果ヘッダ（app.mjs:287-293）だけ `statusBadges` を
呼んでいない。③画面で型式が最大サイズで出るのに「生産終了」バッジが付かない。
**画面で一番目立つのが「発注してはいけない型式」になる**、既存の欠陥。

`discontinuedNote()` は `scopeNote`（ui.mjs:71-76）と同じ全幅警告の形で
app.mjs:263 に並べる。文面案：
「⚠ これは生産終了品です。新規発注はできません。後継は FR-E820-3.7K-1。」

**段4**：`computeCompatibles` の戻り値を `isSuccessor` で2分し、見出しを2つ出すだけ。
**`compat.mjs` と `gate` は変更しない**（判定ロジックを触らず、見せ方だけで
根拠の格差を表現する）。`isSuccessor` は compat.mjs:81 の `a.id === m.successorId` で立つ。

**段5**：サーボの `emptyNote`（drive.mjs:138-148）が前例。
後継が指定されていない生産終了品で、汎用文「電気的に成立しない組み合わせは
除外しています」が出るのを防ぐ。文面は「メーカーは後継機種を指定していません」と、
**指定が無いことを明言する**。

**段6**：スモークで固定すべきシナリオ4つ
1. インバータで `FR-E720-3.7K` を検索 → ②に到達し「生産終了」バッジと後継品チェーンが出る
2. ③で `FR-E820-3.7K-1` が「メーカー指定の後継品」として出る
3. `FR-E820-3.7K-1` を検索したときの候補に旧機種が混ざらない
4. `FR-E720-15K` / `FR-E720-1.5K` で確定せず曖昧一致リストに落ち、容量が併記される

**この分離は旧機種のためだけではありません。** 接触器 `S-N10` は現在
`successorId: MIT-ST10` を持ちつつ、電流窓の近傍と同じ列に混ざっています。
段4の見出し分離は接触器にもそのまま効きます。

---

## 6. 前任Claudeが間違えたこと（同じ轍を踏まないために）

2026-08-20 のセッションで、Claude Code またはユーザーが前任Claudeの誤りを訂正した箇所。

### ① 「1サイズ上までの制限がある」と推測した → 存在しなかった

3回の試行がすべて1サイズ上だったことから「意図的な制限」と結論づけた。
実際は対称な±30%窓で、候補の半分は容量不足側。**実測せずにパターンから推測した。**

### ② 外形寸法を「未取得の宿題」と読み違えた

引き継ぎ書v1の「FR-E800の外形寸法・取付穴ピッチ・質量は未取得（現場では床置き
対処が可能なため、現時点では不要と判断）」を、「取れなかった項目」と読んだ。

**実際はユーザーが過去に3案（A：定格電流のみ／B：A＋定格容量kVA／
C：A＋B＋外形寸法＋質量）から明示的にBを選んだ結果**であり、決着済みの判断だった。
資料から寸法が取れたことに引っ張られて蒸し返しかけた。

**取得できたことと、必要であることは別です。**

### ③ 検査項目の番号を間違えた

「項目5（successors の実在チェック）」と指示したが、実際は項目6だった。
PR #11 で検査が1つ追加され番号がずれていたのを反映し損ねた。
Claude Code は指示に合わせず実際の出力を引用して訂正した。

### ④ 段取りの順序を逆に提案した

「安全側の修正（statusBadges / emptyNote）を先に」と提案したが、
データが無い状態で入れても実機で効果を確認できない。
Claude Code の「データ先・表示後」が正しい。

---

## 7. 懸念事項

### 7-1. 設計文書がリポジトリに残っていない（最優先）

調査結果と設計案は `/root/.claude/plans/quirky-soaring-wall.md` に書かれましたが、
これは **Claude Code のサンドボックス内**で、リポジトリにはコミットされていません。

**セッションが変わると読めなくなります。** §5 に要点を転記しましたが、
比較表・根拠・行番号の詳細は失われています。

→ **次チャットで最初にやること**：段3のプロンプトを出す前に、
設計文書を `docs/` に書き出させることを検討してください。

### 7-2. インバータ70件になったことの副作用

- 一覧・サジェスト・曖昧一致リストに **生産終了品と現行品が混在**します
- 誤発注の防止が、現時点では **バッジの強さだけに依存**しています
- 段3・段4を入れるまで、この状態が続きます

**段3を早めに入れるべきです。**

### 7-3. `_seed` の衝突警告が 199 → 213組に増えた

`_seed` に同じ形名の生成データが18件あるため。警告のみでPASSしているが、
**内容を確認していません。** 過去に1440件の架空型式を生んだのが `_seed` です。

### 7-4. `data/legacy/*.tsv` と `data/inverter.json` の二重管理

段1で昇格させた後も TSV は残す方針（`verify-legacy.mjs` の検査6が
「後継型式が `data/*.json` に実在する」を機械的に守り続け、対応表の腐敗を検出する
台帳として機能する）。

ただし**片方だけ更新される事故**は起こりえます。将来、整合性検査が要るかもしれません。

### 7-5. 決めていないこと（実例が出るまで決めない方針）

| 論点 | 状況 |
|---|---|
| `successors` が複数のケース | 現データ30/30が単数。ただし資料§5に「後継機種なし」の実例あり（FR-E720-5.5K〜15KNC）。`successorChain`（util.mjs:37-49）は1本鎖しか辿れない |
| `(SC)`品（セーフティストップ対応品） | `FR-E720-0.1KSC` と打つとヒット0件。別レコード/note/`compatKey` 的な別名検索のどれにするか未定 |
| アタッチメント型式（FR-E8AT03 等） | インバータではなくオプション品。現在のカテゴリ体系に居場所がない。今は `note` の文章に含まれている |

### 7-6. 私が渡した資料に自信のない箇所

`docs/fr-e700-to-e800-replacement.md` の寸法セクション（現在は使っていない）：

- **FR-E840-5.5K/7.5K の W1=208** は他機種と傾向が違い、誤読の可能性あり（要検証と明記済み）
- 表で「（同上）」と補った行は、資料の表が結合されていたのを前任Claudeが判断したもの
- FR-E820-0.75K-1 の D1 は値を確認できず

**PDFのテキスト抽出であり、図そのものは読めていません。**

### 7-7. FR-E700 の定格出力電流はリポジトリに資料がない

そのため旧機種を電流ベース判定にかけることはできません。
`motorKW` からの換算も、後継E800の値の流用も、資料の「書かれていない値を補わない」に
反するので**埋めないのが正しい**。

必要になった場合は、あなた（チャット側）が FR-E700 のカタログを取得する必要があります。

---

## 8. 作業ループ

```
1. あなた：Claude Code に送るプロンプトを作る
2. ユーザー：iPhoneのClaudeアプリ「コード」タブで新規セッションを開き貼り付け
3. Claude Code：作業してPRを作成
4. ユーザー：結果をスクリーンショットで送る
5. あなた：審査。問題なければマージ手順を案内、あれば修正プロンプトを作る
6. ユーザー：GitHubでマージ → Delete branch
7. 必要なら実機（ホーム画面のアプリ）で動作確認
```

### プロンプトを送る際の設定

- モデル：**Opus 5**
- モード：方針を出させるときは **プラン**、実装させるときは **編集を許可**
- **必ず新規セッション**（前セッションの前提を引きずるため）
- 一度に大きくやらせない

---

## 9. iPhone作業の注意点

- Claude Code の専用アプリはない。Claudeアプリの **「コード」タブ**
- クラウドセッションは **外部ネットワーク遮断**
- `/plugin` `/resume` などターミナル専用コマンドは使えない
- 権限モードは Accept edits / Plan / Auto のみ
- GitHubのモバイル表示では `Pull requests` が `More ▾` の中にある
- 大きいファイルは `Add file → Upload files`
- 利用上限に達することがある（リセット時刻が表示される）
- **チャットの添付ファイル数に上限がある**（今回これで打ち切りになった）。
  スクショを大量に送る場合は、要点を絞るか、こまめにチャットを分ける

---

## 10. 次のチャットで最初にやること

1. **段1+2（PR #13 相当）の結果を正式に審査する**
   - §4-7 に読み取れた範囲を書いたが、前任Claudeは全体を見ていない
   - 特に「削除した検査はゼロ」「検査を新設した」の中身
   - `_seed` の警告 199→213 の内容
   - `drive.mjs` の「現在は…40機種」コメントが更新されているか
2. マージ手順を案内
3. 設計文書を `docs/` に書き出させるか判断（§7-1）
4. 段3のプロンプトを作る
