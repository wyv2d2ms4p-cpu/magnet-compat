# 引き継ぎ書：電気保全向け互換検索アプリ magnet-compat

新しいチャットの冒頭でこのファイルを渡してください。

最終更新：2026-09-03（10カテゴリ目「絶縁変換器」の追加完了時点）

---

## この文書の役割と、他の文書との分担

**この文書は「最初に読む1枚」です。** 経緯と役割と制約だけを書き、
技術的な詳細は持ちません。同じ内容を2箇所に持つと、必ず片方が古くなります。

| 知りたいこと | 読む文書 |
|---|---|
| あなた（Claude）が何をする役か | この文書 §0 |
| なぜこのアプリを作っているか、何を絶対にしてはいけないか | この文書 §1・§2 |
| 設計案C（インバータ旧機種）で何をどう決めたか、なぜそうしたか | `docs/design-c-legacy-inverters.md` |
| 過去に下した判断の理由、過去の失敗 | 同上 6章 |
| 絶縁変換器（insulation）で何をどう決めたか、なぜそうしたか | `docs/design-insulation-converter.md` |
| 作業のときに守る規約 | `CLAUDE.md` |
| データの約束、リポジトリ構成、検査の走らせ方 | `README.md` |
| 旧機種30件の出典と資料の中身 | `docs/fr-e700-to-e800-replacement.md` |
| センサ系の0件表示を精査した実測記録 | `docs/sensor-empty-note-audit.md` |
| 統合時（2026年7月）の計画と結果 | `docs/integration-plan.md` ※当時の記録。現況ではありません |
| 三菱 FR-E800 の型式・定格の一次資料メモ | `docs/mitsubishi-fr-e800-verified.md`／`docs/mitsubishi-fr-e800-ratings.md` |

**検査の項目数をこの文書に書かないでください。** 検査は増え続けるので、
書けば必ず実態からずれます（実際 v2 では 14→17・7→13・63→146 とずれました）。
件数と内訳は `npm run check` の出力に出ます。これは `README.md` が定めた方針です。

---

## 0. あなた（Claude）の役割

このプロジェクトでは、あなたは **Claude Code への指示を設計し、返ってきた結果を審査する側** です。
コードを直接書くのではなく、次の4つを担当します。

### ① Claude Code に送るプロンプトを作る

ユーザーはiPhoneのみで作業しており、長文入力が困難です。
「〜してください」と方針を伝えるのではなく、**そのままコピペできる完成形のプロンプト**を
コードブロックで提示してください。以下を必ず含めます。

- 何を変更し、何を変更してはいけないか
- 守るべき制約（後述の「絶対ルール」）
- 範囲の限定（一度に大きくやらせない。差分が数百行を超えるとiPhoneでレビュー不能）
- 検証方法（`npm run check` を通すこと、壊したら落ちることを確認させること）
- 出力形式（まず方針だけ／実装してPR作成、など）
- **どのブランチで作業し、コミットするかどうか**

最後の項目を書き忘れると、環境の hook が代わりに決めてしまいます。

**指示の適用範囲を、自分が見た実例と一致させてください。**
過去の指示ミスは11件あり、いずれも一部のカテゴリ・一部の実例だけを見て
全体に適用される指示を書いたか、確かめずに承認したことが原因です
（`docs/design-c-legacy-inverters.md` 6-8 節・6-17 節、
`docs/design-insulation-converter.md` 8-8 節）。
カテゴリ横断の変更なら、**全10カテゴリでの挙動を先に調べさせてください。**

**根拠の所在まで指定してください。** 自分が知っていることと、
リポジトリに書かれていることは別です。文書化を指示するときに特に混同しやすい。

### ② 返ってきた結果を審査する

**そのまま承認せず、次を必ず確認**してください。

- 指示していない値が勝手に埋まっていないか（特に出典なきスペック値）
- 「テストが通った」だけでなく「壊したら落ちること」を確認しているか
- **検査の項目数が増えていることは、削除がないことの証明になりません。**
  1件消して5件足せば増えます。`git diff` の削除行そのものを見てください
- 報告の中に、指示していない改善や問題提起が含まれていないか
- **ユーザー（および前任のあなた）の指示が間違っていた可能性はないか**

最後の項目は形式的な注意書きではありません。**記録されているだけで15件以上、
Claude Code が前任Claudeやレビュー側の誤りを訂正しています。**
指示に合わせて辻褄を合わせず、実測に基づいて訂正してくる挙動は正しいので、
それを潰さないでください。

良い点は具体的に指摘し、懸念があれば率直に伝えてください。
**追加0件・変更なしという結果が「正しい結果」である場合があります**（§2）。

### ③ 外部情報の取得を代行する

Claude Code のクラウドセッションは**メーカーの公式サイトやカタログPDFに到達できません。**
npm レジストリと GitHub には到達できます（`npm install` は動きます）。

そのため、**メーカー仕様の調査はあなた（このチャット）が担当**します。
実際にページを取得して確認した内容だけを、Markdownファイルにまとめてユーザーに渡し、
ユーザーがGitHubにアップロード → Claude Code がそれを読んで投入、という流れです。

まとめる際は必ず以下を明記します。

- 出典URL / カタログ番号 / 該当ページ / 取得日
- **取得できなかった項目を「未取得」として明示**（推測で埋めない）
- 表の値と独立に検算できる材料があれば併記する

**ユーザー側から一次資料が来る経路もあります。**
絶縁変換器では、現場の制御盤内の銘板写真と、社内の予備品管理システムの画面が
実在の根拠になりました。この場合も出典の書き方は同じで、
**何を根拠に何を信じたかが後から分かる形**にさせてください
（`docs/design-insulation-converter.md` 1-3 節に実例があります）。

**取得の実務メモ**

- 三菱FA「置換え資料」（資料番号 `BCN-C21002-nnn`）は **ログイン不要で取得できる**
- ただし「カタログ」本体（例：FR-E800カタログ L-06130-H）は **42MB あり、
  web_fetch のサイズ上限を超えて取得できない**
- 置換え資料の一覧ページ：
  `https://www.mitsubishielectric.co.jp/fa/products/drv/inv/ex/renew/renewal/index.html`
- 直リンクの例（実際に取得成功）：
  `https://www.mitsubishielectric.co.jp/fa/document/others/inv/bcn-c21002-214/bcn-c21002-214c.pdf`

### ④ GitHub操作の手順を案内する

ユーザーはiPhoneのブラウザでGitHubを操作します。
「〜をタップ」レベルの具体的な手順で案内してください。

---

## 1. プロジェクトの目的

**電気設備の故障時、復旧時間を短縮すること。**

50年以上前の設備が稼働している現場では、故障した機器が製造中止になっていることが多い。
後継機種や同等機能品を探し、接続方法とパラメータを試行錯誤しながら復旧するため、
長時間停止するケースが多い。この試行錯誤を減らすためのアプリ。

対象カテゴリ：電磁接触器、電磁開閉器、サーマルリレー、インバータ、サーボユニット、
近接スイッチ、フォトスイッチ、超音波センサ、特殊（熱間/光学/導光）、
**絶縁変換器**の10カテゴリ。

**目的から導かれる重要な含意**：現行品を検索できても復旧時間は縮まりません。
**製造中止品から後継に辿り着けること**が、このアプリの存在意義です。
設計案C（段1〜段5）はこの一点に向けた作業でした。

---

## 2. 絶対ルール（これを破ると人が怪我をする）

このアプリは、**設備が止まっている現場で、人が型番を見て発注する**道具です。
存在しない型番を出せば復旧はかえって遅れ、誤った機器を入れれば事故につながります。

1. **出典が確認できない型式は絶対に追加しない**
2. **型番を推測・生成しない**
   - 「S-T10 があるなら S-T13 も」という補完が、過去に **1440件の架空型式** を生んだ
   - 実データは1467件中27件だけで、残り98.2%が組み合わせ生成の産物だった
3. **未取得の項目はキーごと不在にする**（`0` や空文字で埋めない）
4. **記憶や一般知識で補わない**。公式資料に書いてあるかだけで判断する
5. **迷ったら追加せず報告する**

### ルール1の「出典」は、公開URLに限りません

絶縁変換器14件は `evidence.model.srcUrl` を**1件も持ちません。**
実在の根拠は現場写真の銘板2件と、社内の予備品管理システムの画面12件です。

**この型式は公式資料に個別に列挙されていません。** メーカーの仕様書には
型式コード表の組み合わせとしてしか存在せず、`MS3749-A-O25` という綴りは
資料のどこにも書かれていません。

したがって求めるのは「URLがあること」ではなく、
**出典が特定でき、何を根拠に何を信じたかが `srcNote` から読めること**です。
型式の実在と仕様の出典が別系統になることもあります
（実在＝写真、仕様＝メーカー仕様書）。

**ただしルール2は変わりません。** 型式コードから組み合わせを展開してはいけません。
MS3749 のコードは3,888通りに展開でき、`_seed` の1440件事故と同じ構造です
（`docs/design-insulation-converter.md` 8-1 節・8-2 節）。

### 画面の文言にも同じ原則が効きます

アプリが知っているのは**登録された値**であって、現場の実際の条件ではありません。
34A の接触器が付いていても、実負荷が 20A なら 26A で足ります。
DC24V の機器でも、盤に DC24V が来ていれば使えます。

だから画面は事実だけを述べ、「容量不足」「使用できない」とは書きません。
適合の可否を断定すると、データが支えていないことを画面が主張することになります
（`docs/design-c-legacy-inverters.md` 6-10 節）。

### 「追加0件」は失敗ではない

compat-researcher に三菱インバータの調査を指示したところ、クラウド環境から外部に
アクセスできず、検索結果のタイトルとURLしか取れなかった。エージェントは
「そのURLにページがあるらしい、という示唆にすぎない」と判断し、**追加0件で戻ってきた**。
これは正しい挙動です。

---

## 3. 現状

### リポジトリ

`wyv2d2ms4p-cpu/magnet-compat`（Public）

GitHub Pages で公開 → iPhoneのホーム画面に追加して現場で使用。

```
https://wyv2d2ms4p-cpu.github.io/magnet-compat/dist/index.html
```

### 構成と配布形態

構成は `README.md` の「リポジトリ構成」を見てください。**この文書には書きません。**

配布物は `dist/index.html` の単一ファイルで、外部依存はゼロです。
オフライン動作が必須のため、`build.mjs` が全データをインライン展開します。
`build.mjs` は `fetch()` の混入を検査で禁止しています。

### データ

- 実データ **542件**。10カテゴリ
- インバータ70件（現行 FR-E800 が40件、生産終了 FR-E700 が30件）
- **絶縁変換器14件**（MTT の MS3749 系。実在の根拠は現場写真と社内予備品リスト）
- `data/_seed/` に未昇格の生成データ1440件（`provisional`・既定非表示・配布物に入らない）
- `data/legacy/*.tsv` が旧機種30件の原文台帳（4ファイル・30行）
- `data/reference/` に参照データ10件

**件数を更新するときは、この節だけを直せば済むようにしてあります。**
検査の項目数はここに書かないでください。

### 検査

`npm run check` を **exit=0** で通すこと。内訳と項目数は出力に出ます。
検査の中身は `README.md` と各設計文書の検査一覧を見てください。

---

## 4. これまでの経緯

### 2026-08-20：旧機種データの取得と投入

| 確認 | 結果 |
|---|---|
| `FR-E820-3.7K-1`（17.5A）で候補が出るか | ○ FR-E820-5.5K-1（24A）が1件 |
| `FR-E820S-1.5K-1`（単相）に三相が混ざらないか | ○ 混ざらない |
| **逆方向**：`FR-E820-1.5K-1`（三相）に単相が混ざらないか | ○ 混ざらない |
| 同綴り衝突（1.5K / 15K）の警告表示 | ○ 適用モータ容量が併記され警告が出る |

`docs/fr-e700-to-e800-replacement.md` を作成し `main` にマージ済み。

- 出典：**BCN-C21002-214C**「FREQROL-E700 シリーズから FREQROL-E800 シリーズへの
  置換え資料」（全34ページ、取得日 2026-08-20）
- 内容：4電圧クラスの1対1対応表、取付互換アタッチメントの要否、
  FR-E800 の外形寸法・取付寸法、パラメータ移行の要注意点。
  実際の件数は `data/legacy/*.tsv` と `verify-legacy` が持ちます
- 補助出典：セールスとサービス No.856（FR-E700-NC 生産終了、2026年3月）

PR #10〜#12 で、TSV台帳と `docs/fr-e700-to-e800-replacement.md` を整備しました。

### 2026-08-21〜23：設計案C の実装（段1〜段4）

| PR | 内容 |
|---|---|
| #13 | 段1・段2：旧機種30件を `data/inverter.json` に昇格 |
| #14 | 段3：③ヘッダに生産終了バッジ、②に生産終了の警告 |
| #15 | 置換えの条件を `note` から `replacementNote` へ分離 |
| #16 | TSV と `data/inverter.json` の旧機種30件を機械照合 |
| #17・#20 | 設計文書 `docs/design-c-legacy-inverters.md` の作成と6章の追記 |
| #18 | README の件数直書きをやめ、②の枠の並び順を検査で固定 |
| #19 | 段4：③を根拠で2つに分け、主スペックの大小表示を追加 |
| #21 | 段4で守られていなかった約束2件を検査で固定 |
| #22 | この引き継ぎ書の書き直しと、文書間の参照を行番号から節番号へ |

なお v2 の §4-7 には「削除した検査はゼロ」と書かれていましたが、**これは誤りでした。**
実際は18行削除されており、PR #13 のマージ前監査で発覚しています
（設計文書 6-8 節 誤り1）。

### 2026-08-24〜25：段5と、判定の根拠の記録

| PR | 内容 |
|---|---|
| #23 | `smoke` の `S-T10` を使う検査に、選定理由と落ちたときの直し方をコメントで残した |
| #24 | **段5：インバータに `emptyNote` を新設。** あわせてコアの既定0件文がセンサ系にしか当てはまらないことが判明し、そちらへ移した |
| #25 | センサ系の0件文面を16件で実測し、偽になる画面があることを確認。文面を「候補の必要条件を語る形」に差し替え、`tools/test-sensor-empty.mjs` を新設 |
| #26 | `withinWindow` の非対称（下側 −30% / 上側 +42.9%）を3変種で実測し、**現行の式を維持**と決めて記録。対称性検査を接触器・電磁開閉器に拡大 |

### 2026-08-26：検査の穴と、記述の食い違いの是正

| PR | 内容 |
|---|---|
| #27 | `verify-data` の対象カテゴリがハードコードで、**新カテゴリが検査を素通りしていた。** `data/` の走査に変更し、カテゴリの1対1対応と `modelScope` の白リスト検査を追加 |
| #28 | 「カテゴリ追加は2ファイルで完結」が実態と違った（`ADDED_SPEC_KEYS` の宣言も要る）。仕組みは緩めず、記述の側を直した |

### 2026-08-28〜09-02：10カテゴリ目「絶縁変換器」の追加

| PR | 内容 |
|---|---|
| #29 | カテゴリ新設。MTT MS3749 系2件。判定は入力信号・第1出力・第2出力の**完全一致**（窓でも系列でもない） |
| #30 | `note` を現場向けの注意だけに絞り、未登録の理由を `evidence.*.srcNote` へ移した |
| #31 | 出典バッジが長く、390px幅で530件中72件が横スクロールしていた。出典バッジだけを折り返す形に直した |
| #32 | ②の出典行を、操作ボタンより後ろへ移した |
| #33 | 社内予備品リスト由来の12件を追加（計14件）。`specs.option` を配列で新設 |
| #34 | 信号名を銘板の表記で表示（`format` で表示時だけ変換。データは変えない） |
| #35 | `docs/design-insulation-converter.md` を新設 |
| #36 | ③に、電源電圧が違う候補の差を出すパネルを追加 |
| #37 | 検索の正規化で英字 `O` と数字 `0` を同一視（`normLoose` のみ） |

**設計の中身と、なぜそうしたかは各設計文書にあります。** この文書には書きません。

---

## 5. これからやること

設計案Cの段1〜段5はすべて完了しています。**現在の主軸は、予備品リストを起点にした
データの拡充**です。

### 主軸：予備品リストからのデータ拡充

ユーザーの勤務先の予備品管理システムに、機材コードごとの型式リストがあります。
**そこに載っているのは「実際に棚にある物」**なので、カタログから始めるより
目的（復旧時間の短縮）に合っています。旧品も入っています。

手順は次のとおりです。絶縁変換器14件はこの手順で入りました。

1. ユーザーがリストを写真で送る
2. **あなたが型式を書き起こし、一覧で返す** → ユーザーが綴りを確認
3. あなたが仕様を調査し、取れたもの・取れなかったものを分けて報告
4. 判定に使う値が型式から決まらないものは、銘板写真か実物確認を依頼
5. 10〜20件ずつデータ化 → Claude Code に投入 → 実機確認

**一度に全部やらせないでください。** iPhoneでのレビューが成立しません。

### 残っている宿題

| 項目 | 状態 |
|---|---|
| フォトスイッチの英字側の検査が欠けている | PR #38 が持っていた検査1件（photo で `O6H200` と英字で打つ側）。#38 は #37 と競合したため閉じた。`O`/`0` のどちらで打っても同じ結果になることを両カテゴリで対にして見る意図だった。別PRで足せる |
| 設計文書 `design-c-legacy-inverters.md` 4-4・4-5 節の `smoke` 通し番号 | 実測でずれている。同文書 6-16 節が「番号ではなく検査名で」と方針を書いているので、検査名に直す |
| 同 6-17 節の「全9カテゴリ」 | 10カテゴリになった |
| センサ系 `emptyNote` の判断（PR #24・#25）の置き場 | どの設計文書にも無い。`design-c` は0章で対象範囲を宣言しているので範囲外。置き場を決める必要がある |
| 出典バッジの折り返しと位置（PR #31・#32）の判断記録 | 同上 |
| 他カテゴリの電源電圧の差異表示 | フォト468組・近接124組が該当。値が範囲を持つので解析が要る（`docs/design-insulation-converter.md` 参照） |

---

## 6. 前任Claudeが間違えたこと（2026-08-20 分）

### ① 「1サイズ上までの制限がある」と推測した → 存在しなかった

3回の試行がすべて1サイズ上だったことから「意図的な制限」と結論づけた。
実際は対称な±30%窓で、候補の半分は容量不足側。**実測せずにパターンから推測した。**

### ② 外形寸法を「未取得の宿題」と読み違えた

引き継ぎ書v1の「FR-E800の外形寸法・取付穴ピッチ・質量は未取得（現場では床置き
対処が可能なため、現時点では不要と判断）」を、「取れなかった項目」と読んだ。

**実際はユーザーが過去に3案（A：定格電流のみ／B：A＋定格容量kVA／
C：A＋B＋外形寸法＋質量）から明示的にBを選んだ結果**であり、決着済みの判断だった。
資料から寸法が取れたことに引っ張られて蒸し返しかけた。

**取得できたことと、必要であることは別です。**

### ③ 検査項目の番号を間違えた

「項目5（successors の実在チェック）」と指示したが、実際は項目6だった。
PR #11 で検査が1つ追加され番号がずれていたのを反映し損ねた。
Claude Code は指示に合わせず実際の出力を引用して訂正した。

### ④ 段取りの順序を逆に提案した

「安全側の修正（statusBadges / emptyNote）を先に」と提案したが、
データが無い状態で入れても実機で効果を確認できない。
Claude Code の「データ先・表示後」が正しい。


**それ以降に記録された誤りは、次の場所にあります。** 合わせて読んでください。

- `docs/design-c-legacy-inverters.md` 6-8 節・6-17 節（段1〜段4のレビュー、4件）
- 同 6-18 節・6-19 節（窓の非対称性、記述と実態の食い違い）
- `docs/design-insulation-converter.md` 6章（検査が空振りした2件）
- 同 8-8 節（根拠の無い数字を指示に混ぜた）

---

## 7. 懸念事項（未解決のもの）

v2 の §7-1〜7-4 は解決済みのため削除しました。解決の経緯は次のとおりです。

| v2 の懸念 | 解決 |
|---|---|
| 7-1 設計文書がリポジトリに残っていない | PR #17・#20 |
| 7-2 生産終了品と現行品が混在し、バッジの強さだけに依存 | 段3（#14）・段4（#19） |
| 7-3 `_seed` の衝突警告 199→213 の内容が未確認 | PR #13 の監査で評価済み（設計文書 6-5 節） |
| 7-4 TSV と JSON の二重管理 | PR #16 |

### 7-1. 決めていないこと（インバータ）

- **`successors` が複数のケース。** 現データは30/30が単数で、`successorChain` は
  1本鎖しか辿れません
- **`(SC)`品の扱い。** `FR-E720-0.1KSC` と打ってもヒットしません。
  資料の対応表では**セーフティストップ対応品も後継は同一**なので、
  別名検索で同じレコードに寄せる形が資料と整合します。未実装です
- **アタッチメント型式（FR-E8AT03 等）の居場所。** `replacementNote` に分離しましたが、
  アタッチメント自体はどのカテゴリにも属していません
- **取付寸法の変化量。** 資料に W/H の具体値があります（3相200V 3.7K は W=158→128mm、
  3相400V 0.4〜1.5K は W=128→96mm、単相200V 2.2K は H=138→118mm）。未登録です

### 7-2. 決めていないこと（絶縁変換器）

いずれも `docs/design-insulation-converter.md` に理由が記録されています。

- **出力の入れ替え判定。** OUT1 と OUT2 が入れ替わった機器も候補に出す要望がありますが、
  MS3749 では第1出力にラインドライバを選べないため対象が0件です
- **センサ供給電源（`/A`〜`/F`）を判定に使うか。** 該当が14件中1件で、
  代替可否を決める材料がありません
- **在庫数を持つか。** 変動するのでデータに持つと腐ります。持つなら
  「予備品として登録された実績がある」という事実にとどめる形になります

### 7-3. 渡した資料に自信のない箇所


`docs/fr-e700-to-e800-replacement.md` の寸法セクション（現在は使っていない）：

- **FR-E840-5.5K/7.5K の W1=208** は他機種と傾向が違い、誤読の可能性あり（要検証と明記済み）
- 表で「（同上）」と補った行は、資料の表が結合されていたのを前任Claudeが判断したもの
- FR-E820-0.75K-1 の D1 は値を確認できず

**PDFのテキスト抽出であり、図そのものは読めていません。**

### 7-4. FR-E700 の定格出力電流はリポジトリに資料がない

旧機種30件は `ratedCurrentA` と `ratedCapacityKVA` をキーごと持ちません。
資料 BCN-C21002-214C に記載が無いためで、これは意図した状態です。

この結果、旧機種を基準にした③画面では主スペックが `―` になり、
候補の大小判定もできません（`'unknown'` の経路）。

---

## 8. 作業ループ

```
1. あなた：Claude Code に送るプロンプトを作る
2. ユーザー：iPhoneのClaudeアプリ「コード」タブでセッションを開き貼り付け
3. Claude Code：作業してPRを作成し、処理結果を md ファイルで返す
4. ユーザー：その md をこのチャットに添付
5. あなた：審査。問題なければマージ手順を案内、あれば修正プロンプトを作る
6. ユーザー：GitHubでマージ → Delete branch
7. 必要なら実機（ホーム画面のアプリ or Safari）で動作確認
```

**結果はスクリーンショットではなく md ファイルで受け取ってください。**
スクショは添付数の上限に当たりやすく、削除行やJSONを1文字単位で確認できません。
この形式は `CLAUDE.md` に規約として書いてあります。

### セッションを分けるかどうか

| 状況 | 判断 |
|---|---|
| 監査・検証（自分の成果物を評価させる） | **新規セッション** |
| リポジトリの現状を読み取らせる | **新規セッション** |
| 同じPRへの追加修正 | 同じセッションを続ける |
| 直前の発見や実測を前提にする作業 | 同じセッションを続ける |
| 指示の訂正（「その検査では不十分だった」と伝える） | 同じセッションを続ける |

### プロンプトを送る際の設定

- モデル：**Opus 5**
- モード：方針を出させるときは **プラン**、実装させるときは **編集を許可**
- 一度に大きくやらせない

---

## 9. iPhone作業の注意点

- Claude Code の専用アプリはない。Claudeアプリの **「コード」タブ**
- クラウドセッションは **外部ネットワーク遮断**
- `/plugin` `/resume` などターミナル専用コマンドは使えない
- 権限モードは Accept edits / Plan / Auto のみ
- GitHubのモバイル表示では `Pull requests` が `More ▾` の中にある
- 大きいファイルは `Add file → Upload files`
- 利用上限に達することがある（リセット時刻が表示される）
- **チャットの添付ファイル数に上限がある**（今回これで打ち切りになった）。
  スクショを大量に送る場合は、要点を絞るか、こまめにチャットを分ける

追加で、段1〜段4の作業で実際に起きたもの：

- **ローカルの `main` が古いまま止まっていることがある。**
  差分を `origin/main` と比較する規約と、実際に起きた事故は
  `CLAUDE.md` の Git 節にあります
- **チャットの添付が Claude Code 側に届かないことがある。**
  ファイルを渡す指示を出したのに「届いていない」と報告されたら、
  添付操作が漏れています。送信前に添付を確認してください
- **GitHub Pages のキャッシュが強い。** マージ後に実機で確認するときは
  URL の末尾に `?v=8` のようなクエリを付けて開いてください。
  新旧の見分けは、画面のヘッダーではなく変更箇所そのもので判断すること

追加で、絶縁変換器の作業で実際に起きたもの：

- **同じ作業のPRが2つできることがある。** #37 と #38 が同じ題材で作られました。
  マージ前に Pull requests の一覧を見て、同じ題材のものが無いか確認してください
- **新規セッションでリポジトリが選ばれていないことがある。**
  「セッションを開始するにはリポジトリを選択する必要があります」と出たら、
  画面上部のボタンから `magnet-compat` を選んでください

---

## 10. 次のチャットで最初にやること

1. **予備品リストの続きを進めるかを、ユーザーに確認する**（§5 の主軸）
2. 設計文書の宿題（`smoke` の通し番号、6-17 節の「9カテゴリ」）を片付けるかを判断する
